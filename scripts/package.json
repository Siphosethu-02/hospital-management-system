{
  "name": "hms-firestore-migration",
  "private": true,
  "version": "1.0.0",
  "description": "One-off tool: reads the existing MySQL database and writes equivalent Firestore documents. Not part of the deployed app.",
  "type": "commonjs",
  "scripts": {
    "migrate": "node migrate-to-firestore.js"
  },
  "dependencies": {
    "firebase-admin": "^12.3.1",
    "mysql2": "^3.10.2",
    "dotenv": "^16.4.5"
  }
}
